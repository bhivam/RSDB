#ifndef _PARSER_H
#define _PARSER_H

/* TODO Think of what the parsed structure will be.
 *   - Are we looking to convert into byte code for a VM?
 *     - what are the VM parameters, Instruction Set, etc
 *   - Are we going to make ASTs and then interpret the statemetns
 *     using C functions?
 *   - Many other options to think of.
 */  


#endif

